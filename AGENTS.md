# 制作開始前に必読：全指示の理解・実装・検証

<!-- DISTRACTOR-DESIGN-V1 -->
> **選択肢作成前の必須参照：** `rules/DISTRACTOR_DESIGN.md` と `references/OFFICIAL_DISTRACTOR_CASEBOOK.md` を全文読む。公式の観測事例と独自の設計基準を区別し、各候補の本文根拠・もっともらしい理由・決定的なずれ・問いへの適合を確認する。未読・未確認のまま候補を確定しない。既定のレイアウト、SOURCE-FIRST、自然な全訳は維持する。


Version: 1.0 / 2026-09-11

## 0. 必須命令

**このリポジトリを使って教材を作る場合は、関連する制作指示をすべて最後まで読み、全要件の意味・適用範囲・実装方法・検証方法を把握してから制作を開始すること。指示を一部だけ読み、残りを推測して作り始めることを絶対に禁止する。適用される必須要件はすべて守り、違反・未確認が一件でも残る教材を完成版として納品してはいけない。**

「読みました」「理解しました」「指示に従います」という宣言だけでは、この条件を満たさない。以下の読了記録、要件対応表、実装、最終ファイルに対する検証を実行する。読了、実装、検証は別の状態であり、互いの代用にはならない。

本ファイルは制作手順を定める。内容仕様は `MASTER_PROMPT.md`、組版仕様は `LAYOUT_MASTER_PROMPT.md` と各ルールに従う。ここに挙げた例だけで他の指示を置き換えない。

## 1. 適用範囲と指示の扱い

- 新規教材、既存教材の修正、再出力、テーマ変更、他級への変更のすべてに適用する。
- 実行環境の上位指示とユーザーの今回の明示指定を尊重する。このファイルがそれらを上書きするという意味ではない。
- 納品形式、用紙、級、範囲などをユーザーが明示的に変更した場合は、変更された要件だけを置き換え、元の要件・変更指示・理由を記録する。それ以外の指示は維持する。
- 「早く」「簡単に」などを、文字の縮小、検査省略、問題分断、全文和訳省略の許可と解釈しない。都合よく守れる指示だけ選ばない。
- プロンプト自体の更新だけを依頼された場合は、対象ファイルと依存する規則を読み、変更差分・リンク・保存結果を検証する。教材生成やPDF検査は今回の作業対象外と記録する。指示を更新しただけで、既存教材まで修正・検証済みとは言わない。

## 2. ゲートA：全文の取得と読了

教材の文章や生成コードを書き始める前に、次の最新版を取得し、冒頭から末尾まで読む。

1. この `AGENTS.md` と `README.md`
2. `MASTER_PROMPT.md`
3. `LAYOUT_MASTER_PROMPT.md`
4. `rules/FAMOUS_EPISODE_POLICY.md`
5. `rules/NATURAL_JAPANESE_TRANSLATION.md`
6. 指定級の参照資料。2級では `references/EIKEN_G2_LAYOUT_REFERENCE.md`
7. 上記が追加で必読と指定する規則、および今回のユーザー指示

公式問題の最新版確認とページ画像の確認も、MASTERとLAYOUTで指定された範囲を実行する。リポジトリ内の過去の確認日だけを将来の最新版確認の代わりにしない。

ツールに省略・truncated・続きあり等の表示があれば、未取得の続きを取得する。検索結果の断片、目次、READMEの要約、前回の記憶を全文読了の代わりにしない。ファイルを保存しただけでも読了とはしない。

作業用に、ファイルパス、取得元、取得した版またはSHA、読んだ範囲、末尾まで取得できたかを記録する。相互参照で同じファイルが再登場した場合、同一版の全文を既に読了していれば無限に読み直す必要はない。版が変わったら変更部分と影響する要件を読み直す。

必読資料に未読部分が残る場合はゲートA未通過。取得不能の資料を推測で補って作問・組版に進まない。利用できた資料と未確認範囲を明確に区別する。

## 3. ゲートB：理解を実装可能な要件に変換する

全文を読んだ後、制作に適用されるすべての要件を、作業用の要件対応表へ落とし込む。長い思考過程ではなく、仕様と検査事実の短い記録にする。

| 項目 | 必須の記録 |
|---|---|
| 要件ID | 一つの検証可能な条件に対する識別子 |
| 出典 | ファイル名、見出し、必要なら行範囲と短い原文 |
| 適用判断 | 適用／条件付き／対象外。対象外には根拠を記す |
| 解釈・合格条件 | 何が満たされれば合格か。数値・対象・例外を明確にする |
| 実装先と方法 | データ項目、生成コード、スタイル、検査工程等 |
| 検証方法 | 何を、どの最終ファイルから、どの方法で確かめるか |
| 状態 | 未実装／実装済み未検証／合格／不合格／未確認／対象外 |
| 証拠 | 実測値、検査結果、ページ番号と確認画像等。未実施なら空欄 |

作問の形式・語数・語彙水準、事実確認、一意解、誤答、答え漏れ、正答配置、解説、自然な和訳、出力順、組版、文字照合、納品条件まで含める。レイアウトの数項目だけを並べて「全指示を網羅した」としない。

全ての規範的な節・箇条書きが、いずれかの要件に対応するかを原文から逆に照合する。複数条件を一行にまとめる場合も、内訳を検証できる状態にする。文章をそのまま貼り付けるだけでは理解の確認としない。

各要件について、次の四点が説明できなければ理解済みとしない。

- 何を満たす必要があるか。
- 今回のどの部分に適用されるか。
- 具体的にどこへ実装するか。
- 出力後に何を見れば違反を検出できるか。

解釈が曖昧、実装先が空欄、合格条件が不明な要件を放置して制作を開始しない。指示の衝突は上位指示、今回の明示指定、各ファイルの役割分担で整理し、採用根拠を残す。作りやすくするために規則を弱めたり、依頼されていない規則変更をしたりしない。

## 4. 今回の失敗に対する必須の対応例

次は最低限の固定確認例であり、要件対応表全体の代わりではない。数値・役割の詳細はLAYOUTの指定を用いる。

| 指示 | 実装 | 完成前の検証 |
|---|---|---|
| A4縦 | DOCX全セクションの幅210mm・高さ297mmを明示する | 保存後のDOCXとPDF全ページの寸法を実測。PDFは約595.28×841.89pt、各辺の許容差0.5pt以内 |
| 通常の英文本文・設問・選択肢はTimes New Roman系12〜13pt | 役割別に書式を固定。利用可能なフォント解決先を確認する | 実際のPDFのフォント・サイズ・本文役割を照合。セリフ本文をArial等へ置換しない |
| ページに収めるため10pt前後へ縮小しない | ページ型、領域、間隔、内容量の整合を先に確認する | 該当本文の全範囲を測定し、意図しない10.5pt等を検出したら不合格 |
| 問題と全4選択肢を分断しない | 問番号・問題文・4選択肢を不可分ブロックにする | 最終PDFで一問の全要素のページIDが一致するか、座標・画像とともに確認 |
| PDF全ページを画像で確認する | 最終DOCXからPDFと全ページ画像を生成する | 総ページ数と一致する全ページを個別に開いて確認。画像を作っただけでは合格にしない |
| 漢字・かな・長音を失わない | 実在するJP書体と文字データを保持する | 元データ・DOCX・PDFの全文照合と画像の字形確認。抽出順の違いと実際の欠字は分ける |

メール本文など、LAYOUTが別書体を指定する役割までTimes系に統一しない。見出し・フッター等の許可されたサイズ差と、本文の意図しないサイズ差を区別する。特定のフォント商品名を使えない場合の代替は既存規則の範囲に限り、公式と同一であるとは説明しない。

## 5. ゲートC：実装と内容の固定

ゲートA・Bを通過した後、要件対応表に基づいて作問・生成処理・組版を実装する。コードを書いたという理由だけで各要件を合格にしない。状態は「実装済み未検証」にする。

- 既存テンプレートや過去の生成コードも、今回の全要件に照らして確認する。以前使えたことを合格根拠にしない。
- 用紙、フォント、サイズ、行間、改ページをライブラリの既定値へ任せない。
- titleとparagraphsを区別し、人名の前方一致で本文を見出しにしない。
- 選択肢、正解ID、解答表、解説は同じ確定データから生成する。番号だけを後から置換して整えない。
- 内容を固定した版を記録し、組版のために本文や訳を黙って削らない。内容を変更した場合は内容確認に戻す。
- 収まらない、必要なフォントが使えない、検査方法がない場合は、先に実装方法を修正する。品質基準を下げて通過させない。

## 6. ゲートD：最終ファイルに対する検証と納品判定

内容と組版の両方について、全ての適用要件を検証する。設定値・ファイル保存成功・ページ数だけで合格としない。LAYOUTに定める機械検査と全ページの個別目視確認は両方実施する。

証拠には、対象の最終ファイルの版またはハッシュ、実測値、検査結果、確認したページ番号と画像名を結びつける。証拠が空欄、古い版の証拠しかない、実行できていない場合は「未確認」であり「合格」ではない。

```text
完成版として納品可能 =
  必読資料を全文読了
  AND 全適用要件の解釈・実装・検証方法を確定
  AND 全適用要件を実装
  AND 全適用要件を最終版で検証し合格
  AND 違反・未確認が0件
```

これは制作担当者が実行すべき判定規則であり、この文書を追加しただけで自動検査プログラムが動作するという意味ではない。

DOCX、内容データ、フォント、スタイル、ページ割り、変換環境のいずれかを変更したら、依存するPDF・画像・検査結果を旧版として無効にする。内容確認の影響範囲を再検証し、最終WordからPDFと全ページ画像を再生成して出力検査をやり直す。

一つでも不合格・未確認があれば、修正して再検査する。「ほぼ合格」「内容はよい」「時間がない」を理由に完成扱いへ進めない。環境上解消できなければ、実際にできた範囲と未確認項目を報告する。途中成果を提示する場合も未完成であることを明示し、完成品に見せかけない。

「本番と一致」「全ページ確認済み」「GitHubの指示をすべて満たす」「修正済み」等は、それぞれを実際に裏づける検証があるときだけ用いる。要件対応表へ合格と書き込むだけの形式的な検査を禁止する。

## 7. 記録の扱いと再開時の確認

読了記録、要件対応表、検査画像、検査結果は作業用の資料とし、生徒用教材へ混ぜない。作業記録を公開リポジトリへ無断で追加しない。監査を依頼された場合は、必要な事実と確認結果を説明する。

作業を再開するときは、読んだ仕様の版、確定内容の版、実装状況、最終PDFと画像の版、未確認項目を確認する。文脈から抜けた指示は再取得し、記憶で埋めない。検査済みという前回の文章だけを証拠にしない。

この指示は遵守を必須にするが、文書を追加するだけで将来の全実行における違反ゼロを技術的に保証するものではない。実行していない自動検査や、存在しない納品ブロック機能を実装済みと説明してはいけない。
