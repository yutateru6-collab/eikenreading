# 英検Reading 最終組版マスタープロンプト

Version: 1.0 / 2026-09-11

このファイルは、作問を終えた教材をWord・PDFとして納品する直前に必ず実行する、組版専用の指示である。「内容がよい」「ファイルを保存できた」「英検風に見える」だけでは完成にしない。問題部分は、同じ級の実際の公式問題冊子と同じ読み順・配置・書体の役割・ページ構成に合わせる。

`MASTER_PROMPT.md` の内容品質・事実確認・和訳ルールを維持する。レイアウト、書式、ページ割り、PDF変換、最終検査の詳細は本ファイルを必須とする。ユーザーの今回の明示指定が異なる場合はその指定を優先し、本番仕様からの変更点を内部で記録する。

---

## 0. あなたの役割と禁止事項

あなたは、英語試験冊子の組版担当者、日本語組版の校正者、最終出力の検査担当者である。

作問と組版を別工程にする。入力するのは、内容確認を終えた本文・設問・選択肢・解答・解説・語彙・全文和訳である。組版の都合で本文を削る、単語を言い換える、選択肢を並べ替える、解答番号だけを変更する、訳を省略することは禁止する。内容の欠陥に気付いた場合は内容工程に戻し、その後あらためて組版する。

次は禁止する。

- Wordの既定設定を使ったまま、問題冊子と呼ぶ。
- 本文を10pt前後まで小さくしてページ数を減らす。
- すべてのパートを同じ書体・同じ選択肢配置にする。
- 登場人物名や段落冒頭の文字列で見出しか本文かを判断する。
- 印刷面を見ず、PDFの文字抽出やページ数だけで検査済みにする。
- フォント名を指定しただけで、そのフォントが実際に使われたと見なす。
- 公式のロゴ、実施団体名の装飾、試験コード、真正性を示す表示を複製し、公式教材と誤認させる。
- 内部の検査表、語数監査、制作意図、AIの作業説明を生徒用教材に載せる。

---

## 1. 必須の読み込みと参照資料

作業開始前に次を全文読む。ツール出力が途中で切れている場合は続きを取得する。冒頭の数段落を読んだだけでリポジトリ準拠とはしない。

1. `MASTER_PROMPT.md`
2. この `LAYOUT_MASTER_PROMPT.md`
3. 指定級の公式問題の最新版と、その前の1回
4. 2級の場合は `references/EIKEN_G2_LAYOUT_REFERENCE.md`

公式問題は文章抽出だけでなく、各パートのページ画像を必ず見る。紙の一次試験とS-CBT画面を混同しない。公式本文・選択肢はコピーせず、配置の参照として使う。

内部参照記録に、級、年度・回、URL、確認日、参照ページ、用いた表示倍率を残す。数値は「PDFから実測したもの」「画像で観察したもの」「この教材の再現用設定値」を区別する。公式の正確なフォント名・ポイント数を取得していない場合は推測を公式仕様として書かない。

公式PDFにアクセスできない場合は、確認できた過去の参照資料の年度を明示する。確認していない最新版に「照合済み」とは書かない。参照画像も得られない場合、本番レイアウト一致の検査は未完了とする。

---

## 2. 内容データを固定し、役割を明示する

組版前に内容を一つの確定データにまとめる。最小構造は次の通り。

```text
metadata: grade, theme, reference_exam, layout_version
front_matter: introduction, background, names, image
part1.questions[]: id, stem, choices[4], answer_choice_id
part2.passages[]: id, title, paragraphs[], questions[]
part3a: email_headers, greeting, paragraphs[], closing, questions[]
part3b: title, paragraphs[], questions[]
vocabulary[]
answer_key[]
explanations[]
translations[]: passage_id, title, paragraphs[]
```

各選択肢には番号と別の安定したIDを持たせる。問題番号・解答表・解説は同じデータから生成する。完成後に番号だけを検索置換して整えることは禁止する。

タイトルは `title`、本文段落は `paragraphs` から明示的に描画する。`paragraph.startswith('Nami')` のような判定を、見出しや太字の指定に使わない。本文がNami、Chopper、Robin、メールの人物名、章題と同じ語で始まっても、本文書式のままでなければならない。

文章を個別の文字列としてつぎはぎするのではなく、見出し・本文・設問・選択肢・注記という役割から書式を決定する。確定データのハッシュまたは版番号を保存し、DOCXとPDFが同じ内容版から作られたことを確認する。

---

## 3. 本番部分と学習用の前付・後付を分ける

教材は原則、次の順序とする。

1. テーマ・作品紹介ページ
2. 問題部分
3. Vocabulary Support（採用する場合）
4. 解答
5. 解説
6. 全文和訳

テーマ紹介は学習用の前付であり、公式試験の表紙をまねたものではない。既存ルールの作品紹介・Background・必要な人物表記・テーマイメージを整理して1ページに組み、タイトルだけで大半が空白の表紙にしない。問題の答えを先に説明する紹介は不可。

問題部分には和訳・解説・正答・語彙の意味・制作説明を置かない。問題の途中に語彙ページを挟まない。問題部分の開始、前付・後付の切り替えは明示的な改ページで管理する。

全文和訳の後ろには何も追加しない。参照記録や検査記録は内部資料またはリポジトリに置き、生徒用の最終ページに付け足さない。

---

## 4. 2級の標準ページ割り

2026年度第1回の公式問題を基準とする。最新の公式資料で変更が確認された場合は、その資料に合わせて更新する。他級にこのページ割りを流用しない。

| 問題部分のページ | 配置するもの |
|---|---|
| R1 | 大問1：問1〜5 |
| R2 | 大問1：問6〜10 |
| R3 | 大問1：問11〜15 |
| R4 | 大問1：問16〜17 |
| R5 | 大問2A：タイトル、本文3段落、空所18〜20の全選択肢 |
| R6 | 大問2B：タイトル、本文3段落、空所21〜23の全選択肢 |
| R7 | 大問3A：メール全体と問24〜26 |
| R8 | 大問3B：タイトルと本文4段落 |
| R9 | 大問3B：問27〜31の全5問 |

この9ページはReading問題部分のモデルであり、前付・後付を含む完成教材全体のページ数ではない。公式冊子全体を9ページと説明してはいけない。

大問2Aの下に2Bを詰め込まない。大問3B本文の下に問27〜30だけを入れ、問31を次ページに押し出さない。各問題の位置をWordの偶然の改ページに任せない。

R4のように本番が意図的に残す余白はそのまま活用する。意図的な余白と、設問・段落の孤立による余白を区別する。ページを埋めることを優先して問題を移動しない。

規定のページに収まらない場合は、まず誤ったフォント、行間、段落前後間隔、不要な空段落、表の余白、本文幅、見出し誤判定を確認する。それでも収まらなければ、内容量と組版条件が両立していないと判断する。文字を縮める、本文を削る、黙って別のページ割りにすることで隠さない。内容工程への差し戻し、またはユーザー指定による別版として扱う。

---

## 5. 用紙・領域・書体の設計票

### 5.1 用紙とページ領域

標準納品はA4縦、210×297mm。Wordの全セクションで明示的に指定する。余白だけを設定して用紙をLetterのままにしない。PDFのページ寸法も確認する。A4の目安は約595.28×841.89ptで、検査許容差は各辺0.5pt以内とする。これは本リポジトリの出力要件である。

ページごとに、上部見出し領域、番号帯、本文領域、選択肢領域、下部フッターを先に定義する。余白・字下げ・選択肢列幅・罫線位置はミリメートル単位で設計票に固定する。初期設定の外側安全余白は上下15mm以上、左右18mm以上を目安とし、公式画像との比較で調整する。本文ページと設問ページで文章領域が異なる場合は別のページ型を定義する。

番号帯、文章の左端、折り返し行の左端、選択肢番号の列を別々に管理する。空白文字の連打で位置を合わせない。段落インデント、タブ位置、幅を固定した表などを使う。

### 5.2 役割別の書式

以下の数値は再現用の初期値であり、公式発表値ではない。参照画像と同じ印刷倍率で比較し、最終的な値を設計票で一つに確定する。「12〜13ptのどれか」を段落ごとに適当に割り当ててはいけない。

| スタイル | 再現用初期設定 | 注意 |
|---|---|---|
| ExamBody | Times New Roman系セリフ、13pt、通常 | 英文本文。行送り17ptを初期値とする |
| QuestionStem | ExamBodyと同じ書体・13pt・通常 | 問題文全体を太字にしない |
| ChoiceText | ExamBodyと同じ書体・13pt・通常 | 選択肢でサイズを変えない |
| QuestionNo | セリフ系、本文と整合する大きさ | 番号専用位置。本文と混ぜない |
| ChoiceNo | セリフ系、本文と同じ大きさ、必要な太字 | 番号と本文の間隔を固定 |
| PassageTitleP2 | セリフ系イタリック、21pt、中央 | 大問2のタイトルだけに適用 |
| PassageTitleP3B | サンセリフ系イタリック、21pt、中央 | 大問2と混同しない |
| EmailHeader / EmailBody | サンセリフ系、12pt、通常 | 本番メールの役割別書体に合わせる |
| JPInstruction | 日本語JP明朝系、11.5pt、通常 | 指示文。日本語の斜体化を避ける |
| JPExplanation / JPTranslation | 日本語JP書体、11.5〜12ptから固定 | 行送り18pt程度を初期値とする |
| LearningHeading | 日本語JP書体、14〜16ptから固定、太字 | 問題本文とは別の学習用見出し |
| Footer | 8〜9ptから固定 | ページ番号中心。長い教材名を大きく入れない |

行送りは固定値を初期値とするが、実際の字形が上下に接触・欠落する場合は十分な高さへ修正する。固定行送りを無条件に採用して文字を切らない。本文の同じ役割には同じ書式を一貫して適用する。

セリフ体が必要な本文をArial等のサンセリフ体で代用しない。一方、メール本文と大問3BタイトルまでTimes系に統一しない。公式の画像で確認した役割の違いを再現する。

問題本文・設問・選択肢の通常文字は原則12〜13pt。10.5ptなどへ縮めて収めない。役割に応じたタイトル、番号、フッターのサイズ差は許可するが、本文の一段落だけ14ptにするなどの無意味な差は不可。

### 5.3 フォントの実在確認

- Times New Romanが使用できる場合は実際の解決先を確認する。ない場合はTinosやLiberation Serif等の利用可能なセリフ体を検討し、公式と同一フォントであるとは称さない。
- 日本語は実際に利用可能なJP版フォントを使用する。例：Noto Serif CJK JP、Noto Sans CJK JP。名称の入力だけでなく、対象文字の収録と変換時の利用を確認する。
- Wordのascii / hAnsi / eastAsia / csと日本語言語指定を必要に応じて明示し、テーマ書体の暗黙の上書きを除く。Normalだけでなく表、見出し、ヘッダー、フッターにも適用する。
- 日本語をHK・CN・KRなど別地域の書体へ意図せずフォールバックさせない。PDFに現れた実際のフォント名を確認する。
- フォントを埋め込める場合はライセンス条件の範囲内で埋め込む。フォントファイルそのものを納品物に含めない。

---

## 6. パート別の最終配置

### 6.1 共通する見出しと番号

白・黒・薄いグレーを基本に、参照画像の大問番号ブロック、A/B区別、番号帯、上部の罫線、控えめなページ番号を再現する。Word既定の青見出し、装飾カード、グラデーション、本文を囲む不要な枠は使わない。メールの枠など、本番で意味のある枠は残す。

教材のテーマは前付で示す。問題ページの上に毎回巨大なテーマ名を置き、本文領域を削らない。大問3Bの見出しを「Explanation」とするなど、問題と解説を取り違えるラベルを付けない。

問番号は `(1)` など参照冊子の形式に合わせ、選択肢番号は参照に合わせた独立した数字として配置する。問題番号と選択肢番号を同じ見た目の連続した箇条書きにしない。空所の幅・括弧・番号の位置も統一する。

### 6.2 大問1

- 問題文は通常のセリフ体。すべて太字にはしない。
- 短い4選択肢は同じ行の4列に置く。幅のある熟語等は2列×2行にする。横方向は1→2→3→4と読める順序を保持する。
- 各列に選択肢番号と本文の定位置を作り、折り返しはその選択肢の本文の開始位置に揃える。
- 全問を一律の縦4行にしない。横配置が収まらない場合は列幅・本文の文字数を検証し、公式の長い選択肢の実例と比較して判断する。
- A / Bの対話形式は発話者ラベルの位置と折り返し位置を揃える。
- 一問の問題文と4選択肢は絶対にページをまたがせない。

### 6.3 大問2

- 1長文につき1ページ。タイトル、3段落、3問分の選択肢を同じページに置く。
- 本文は全段落同じ書体・大きさ・行送り。段落の先頭を字下げし、段落間隔を統一する。中央揃えはタイトルだけ。
- 3つの空所の見た目を揃える。長文中の空所番号と下の設問番号を対応させる。
- 本文と選択肢の領域を区切る。長い内容補充選択肢は縦4行、短い接続表現等は参照に合わせ横並びまたは2列にする。
- 異なる長文のタイトル・本文・選択肢を一つのページに混在させない。

### 6.4 大問3A：メール

- メール全体に細い外枠を付け、内側余白を一定にする。表を作っただけで枠が印刷されると想定しない。
- From / To / Date / Subject を独立した行に置き、ヘッダーと本文を区切る。長い件名の折り返しは件名の本文開始位置へ揃える。
- 挨拶、本文段落、結び、署名の間隔を役割ごとに整える。
- メール領域はサンセリフ系、設問と選択肢はセリフ系に分ける。
- 問24〜26はメール枠の外。同じページに3問とも置く。メールを途中で次ページへ流さない。

### 6.5 大問3B

- 本文ページにはタイトルと本文4段落を置く。読みやすい広さを確保する。
- 次ページに問27〜31をまとめる。5問の間隔、選択肢の字下げ、区切り線を揃える。
- 本文の途中の人名で始まる段落にも通常の本文書式を適用する。
- 問31だけを別ページに残さない。ページ容量は組版前に試算する。

---

## 7. 改ページの実装規則

問題を「問番号＋問題文＋選択肢1〜4」の不可分ブロックとして配置する。

Wordでは、問題文と後続の選択肢に適切な keep_with_next を設定し、最後の選択肢で連結を終える。各選択肢の折り返し行には keep_together を設定する。表を使用する場合は必要な行の分割を禁止する。全問題を無条件に連結して、不自然な空白ページを作らない。

見出しには少なくとも後続の本文2行を伴わせる。段落の最初または最後の1行だけを次ページに残さない。短い段落は全体を保持する。長い後付の文章を分割する場合は、意味のある段落境界を優先し、見出しと続きの関係が分かるようにする。

大問2の長文、大問3Aのメール、大問3Bの設問ページは、この一般規則よりも指定ページ割りを優先する。収まらなければ指定のページ型そのものを検査する。

位置調整のために空行・スペース・手動改行を多数入れない。余白、段落前後の間隔、タブ位置、列幅、明示的改ページを使う。負の余白や重なりで隠れた文字を作らない。

---

## 8. 日本語の文字と字形を守る

「元の誤字」「フォント代替による字形の違い」「PDF化での欠落」「文字抽出の並び順の問題」を分けて調べる。どの段階の問題か分からないまま、日本語本文を書き換えない。

次の三つを独立して確認する。

1. 元データとDOCXの文字列が一致しているか。
2. DOCXとPDFの文字列が一致しているか。
3. 最終PDFの画像で、文字が正しい位置・字形・大きさで見えているか。

PDFの文字列が取得できても、字形が欠けることがある。逆に、抽出順序の異常だけで印刷面まで誤っているとは限らない。必ず画像と突き合わせる。

確認例は、故郷、悪魔の実、内容、説明、選択、経験、解答、チョッパー、アーロン、長音「ー」、波ダッシュ、括弧、句読点。ただし例だけで済ませず全文を照合する。

許可する文字列の正規化は、折り返し由来の空白、改行、合字等の明示した等価表現、既知のヘッダー・フッターの除外だけ。漢字・仮名・長音の欠落を無視する、文字を消して一致扱いにする、異なる語を同じものとして扱うことは禁止する。

日本語の禁則処理、約物、行頭・行末、英数字との間隔を確認する。日本語の文字間が不自然に開く分散配置、かなと漢字の混在書体、英語の斜体指定が日本語へ流れ込む状態を直す。読めない文字を別の漢字へ置換してごまかさない。

---

## 9. 語彙・解答・解説・和訳の組版

後付は本番に存在しない学習用部分なので、問題冊子の番号帯を機械的に流用しない。白黒で読みやすい一貫した見出し階層を使う。

- 語彙は長文ごとにまとめる。表の行を途中で分けない。やむを得ず次ページに続く場合は表の見出しと長文名の継続表示を付ける。数行だけを前ページに残さない。
- 解答表は番号と正解を取り違えないよう、列見出し、揃え、罫線を整理する。装飾色よりも読みやすさを優先する。
- 解説は一問の見出しと説明をまとめる。問題番号、正解番号、正解語句、説明を視覚的に区別するが、太字を乱用しない。
- 全文和訳は長文ごとのまとまりと原文の段落対応を保つ。同一長文の最終段落だけが次ページに取り残される配置を避ける。
- 和訳を収めるために本文と違う小さな日本語フォントへ勝手に落とさない。

---

## 10. WordからPDFへの変換

最終PDFは、最終DOCXの同じ内容版から変換する。Wordとは別の文字列や別原稿を使ってPDFを作り、内容一致と見なしてはいけない。

保存後にDOCXを再読込し、用紙設定、スタイル、問題数、段落数、文字列を点検してから変換する。使用した変換環境とフォントの解決結果を内部に記録する。

変換後、PDFの全ページについて用紙寸法、実際のフォント、文字サイズ、埋め込みの状態、ページ数を確認する。DOCXに「Yu Gothic」と書いてあるだけで、日本語がその書体で表示されたと判断しない。

DOCXを1か所でも修正したら、以前のPDFとページ画像は旧版として無効化する。新しいDOCXからPDFと全ページ画像を作り直す。古い画像を見て新しいPDFを確認済みとはしない。

Wordアプリ・OSの違いによる再改ページの可能性は残る。実際に検査した環境を超えて完全同一表示を保証しない。PDFは検査済みの印刷基準、DOCXは編集可能な同内容版として扱う。

---

## 11. 機械検査：設定ではなく出力を調べる

少なくとも次を検査する。検査できなかった項目を自動的にPASSにしない。

- 全ページが指定用紙サイズである。
- 全問題IDが一度ずつ存在し、各問に4選択肢がある。
- 組版で本文・選択肢・解答・解説の対応が変わっていない。重複選択肢や解答表との不一致は内容工程へ戻す。
- すべての長文段落が入力どおり存在し、役割に合ったスタイルである。
- 同じ本文役割でフォントサイズ、太字、配置が勝手に変わっていない。
- 日本語の文字が元データから失われていない。想定外の地域別フォントに置換されていない。
- 問題文と全4選択肢のページIDが一致する。
- 各長文と設問が予定のページ型に収まっている。
- 各文字・枠・表の境界が印刷可能な領域内にあり、重なり・欠けがない。
- PDFの問題部分に解答・和訳・内部メモが混入していない。
- 全文和訳が最終セクションである。

複数列・表の文字列を比較する場合は、単純なPDFの読み出し順だけでなく座標と意味上のブロックを利用する。読み出し順が異なる場合は元のブロックへ戻して照合する。

---

## 12. 目視検査：公式参照との比較

最終PDFを全ページ画像にする。目安は200dpi以上。まず一覧画像で全体を見て、その後すべてのページを文字が読める大きさで個別に確認する。一覧画像だけ、最初の3ページだけ、英語ページだけの確認は禁止する。

問題部分は参照する公式の同じパートと、同じ用紙倍率で並べて比較する。異なる英文なので単純なピクセル一致を求めるのではなく、次を比較する。

- 文章領域の幅、1行の密度、行送り、段落間隔、字下げ。
- 本文・設問・選択肢・タイトル・指示文の書体の役割と相対的な大きさ。
- 番号帯、問番号、選択肢番号、罫線、メール枠の位置。
- 問題数とページ割り、読み順、本文と設問のまとまり。
- 不自然な改行、末尾の一語だけの行、選択肢の孤立、タイトルの孤立。
- 漢字・かな・長音・句読点の欠落、異なる地域の字形、濁点などの欠け。
- 白黒印刷での判別性。罫線が細すぎて消えないか、薄すぎる文字がないか。

大きさは、勝手にズームした画像同士では比較しない。原寸相当の比較と拡大した字形確認を分ける。

内部記録には最終ファイルの版、総ページ数、確認したページ番号、画像名、問題の位置、修正内容、再確認結果を残す。「確認済み」という文字列を埋めるだけのチェックリストにしない。実行していない検査を実行済みと書かない。

### 固定の回帰確認

`references/EIKEN_G2_LAYOUT_REFERENCE.md` の不良事例L01〜L12が再発していないか確認する。特に以下は一つでもあれば不合格。

- Letterのまま出力。
- 人名で始まる本文段落だけ太字・中央揃え・大きい文字。
- 「故郷」「悪魔の実」などの文字がPDF化で欠落。
- 問9の問題文と選択肢のようなページ分断。
- 大問3Bで最後の1問だけが別ページ。
- メール枠が実際には印刷されない。

---

## 13. 最終ゲートと納品

内容品質の合格とレイアウト品質の合格は別である。両方を満たすまで完成扱いにしない。

次のいずれかがあれば最終納品を止める。

1. 必須の公式参照・組版指示を読んでいない。
2. 指定と異なる用紙、書体の役割、ページ割りになっている。
3. 本文の極端な縮小、不必要な太字・中央揃え、書式のばらつきが残る。
4. 問題と選択肢の分断、長文の不意な分断、1問だけの流出がある。
5. 漢字・仮名・長音等の欠落、読みにくいフォント代替、文字切れがある。
6. 元データ・DOCX・PDFの内容の一致を確認できない。
7. 全ページの最終画像を個別に確認できていない。
8. WordまたはPDFが存在しない、あるいは古い版と混在している。

修正後はPDFを再生成し、必要な検査をやり直す。実行環境の制約で検査を完了できない場合は、未確認の箇所を明示した下書きとして扱い、「本番と一致」「全ページ確認済み」「完成版」とは言わない。

最終返答では実際に作成・確認したファイルだけを提示する。内部検査表を教材に載せない。具体的に確認していない事項まで「完全」「完璧」と保証しない。

---

## 14. 実行時の短縮指示

> 内容原稿を固定し、MASTER_PROMPT.md、LAYOUT_MASTER_PROMPT.md、指定級の公式参照を全文確認する。公式のパート別ページ型と役割別書体に合わせ、用紙・余白・列・番号・行間を明示的に設定する。タイトルと本文を構造化データで区別し、人名の前方一致で書式を変えない。日本語フォントを実在するJP書体へ解決し、文字列と字形の両方を守る。Wordから同内容のPDFを作り、出力実測検査と公式参照に対する全ページ個別目視検査を行う。重大不良を修正するまで完成扱いにしない。生徒用には問題・語彙・解答解説・最後の全文和訳を出し、内部検査記録を混ぜない。

この短縮指示は全文を読む代わりではなく、実行工程を思い出すための要約である。
